"""Behave step definitions for shared Gherkin under ``packages/sdk/features``.

Other language SDKs will add their own runners; these steps execute the Python
adapter against the same ``.feature`` files.
"""
